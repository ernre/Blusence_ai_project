FROM python:3.11-slim

WORKDIR /app
COPY pyproject.toml README.md /app/
COPY src /app/src
RUN pip install --no-cache-dir -e .
CMD ["uvicorn", "vpe.serving.api:app", "--host", "0.0.0.0", "--port", "8000"]
