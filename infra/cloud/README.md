# GPU Cloud IaC Stubs

This folder is reserved for Terraform or Pulumi modules that provision:

- GPU inference nodes.
- Redis-compatible queues.
- Postgres metadata storage.
- S3-compatible artifact buckets.
- Container registry and deployment wiring.
