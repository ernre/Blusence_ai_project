# Core

The core package contains VPE inference components: garment conditioning,
decoupled attention hooks, pose branches, multi-view aggregation, LoRA loading,
and the production try-on engine facade.
